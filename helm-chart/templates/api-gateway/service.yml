apiVersion: v1
kind: Service
metadata:
  name: api-gateway
  labels:
    app: api-gateway

spec:
  selector:
    app: api-gateway

  ports:
    - name: http
      port: {{ .Values.apiGateway.servicePort }}
      targetPort: {{ .Values.apiGateway.containerPort }}