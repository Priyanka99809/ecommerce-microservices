apiVersion: v1
kind: Service
metadata:
  name: order-service
  labels:
    app: order-service
spec:
  selector:
    app: order-service
  ports:
    - name: http
      port: {{ .Values.orderService.servicePort }}
      targetPort: {{ .Values.orderService.containerPort }}