apiVersion: v1
kind: Service
metadata:
  name: order-service
spec:
  selector:
    app: order-service
  ports:
    - port: {{ .Values.orderService.servicePort }}
      targetPort: {{ .Values.orderService.containerPort }}