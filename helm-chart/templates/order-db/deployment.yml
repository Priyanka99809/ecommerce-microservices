apiVersion: apps/v1
kind: Deployment
metadata:
  name: order-db
spec:
  replicas: {{ .Values.orderDB.replicas }}

  selector:
    matchLabels:
      app: order-db

  template:
    metadata:
      labels:
        app: order-db

    spec:
      containers:
        - name: order-db
          image: "{{ .Values.orderDB.image }}:{{ .Values.orderDB.tag }}"
          ports:
            - containerPort: {{ .Values.orderDB.containerPort}}
          env:
            - name: MYSQL_DATABASE
              valueFrom:
                configMapKeyRef:
                  name: app-config
                  key: ORDER_MYSQL_DATABASE

            - name: MYSQL_ROOT_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: app-secret
                  key: MYSQL_ROOT_PASSWORD

          startupProbe:
            exec:
              command:
                - mysqladmin
                - ping
                - "-h"
                - "127.0.0.1"
                - "-uroot"
                - "-proot"
            periodSeconds: 5
            failureThreshold: 30

          volumeMounts:
            - name: mysql-storage
              mountPath: /var/lib/mysql

      volumes:
        - name: mysql-storage
          persistentVolumeClaim:
            claimName: {{ .Values.orderDB.claimName }}