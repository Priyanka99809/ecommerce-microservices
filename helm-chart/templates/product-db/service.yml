apiVersion: v1
kind: Service
metadata:
  name: product-db
spec:
  selector:
    app: product-db
  ports:
    - port: {{ .Values.productDB.containerPort}}
      targetPort: {{ .Values.productDB.containerPort}}